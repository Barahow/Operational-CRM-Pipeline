version: 2

sources:
  - name: crm_warehouse
    database: crm-warehouse   # GCP project ID
    schema: crm_warehouse      # dataset where Airbyte loads raw tables
    description: "Raw tables loaded by Airbyte into crm_warehouse dataset"
    tables:
      - name: dim_customer
      - name: dim_product
      - name: dim_ticket
      - name: fact_sales
      - name: fact_support_ticket
